DATABASE_URL=""

QR_SCAN_IP_SALT=""

# Used by compose.yml to configure the `db` service and to build the
# app's DATABASE_URL when running via `docker compose`.
POSTGRES_USER=""
POSTGRES_PASSWORD=""
POSTGRES_DB=""
